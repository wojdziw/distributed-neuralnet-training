import numpy as np

generate_white_input(dataset_size, no_filters, image_size, labels, output_path):
	images = 

	return 0

dataset_size = 400
no_filters = 96
image_size = [13,13]
labels = np.load('image_data')